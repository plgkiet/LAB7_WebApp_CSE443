@model LAB3_WEBAPP.Models.Category
@{
    Layout = "AdminLayout";
    ViewBag.Title = "Chỉnh sửa danh mục";
}

<h2 class="mb-4">Edit</h2>

<form asp-action="Edit" method="post" enctype="multipart/form-data">
    <input type="hidden" asp-for="CategoryId" />

    <div class="mb-3">
        <label asp-for="Name" class="form-label"></label>
        <input asp-for="Name" class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Description" class="form-label"></label>
        <textarea asp-for="Description" class="form-control"></textarea>
    </div>

    <div class="mb-3">
        <label asp-for="Avatar" class="form-label">Current Image</label><br />
        @if (!string.IsNullOrEmpty(Model.Avatar))
        {
            <img src="~/images/@Model.Avatar" alt="Avatar" style="height: 80px;" />
        }
        else
        {
            <span class="text-muted">No Image</span>
        }
    </div>

    <div class="form-group mb-3">
        <label asp-for="Avatar" class="control-label"></label>
        <input asp-for="Avatar" class="form-control" placeholder="Filling file path (VD: images/category1.jpg)" />
    </div>

    <div class="form-check mb-3">
        <input asp-for="IsActive" class="form-check-input" />
        <label asp-for="IsActive" class="form-check-label">Activate Category</label>
    </div>

    <button type="submit" class="btn btn-success">Save Changes</button>
    <a asp-action="Index" class="btn btn-danger">Cancel</a>
    <a asp-action="Index" class="btn btn-secondary">Back</a>

</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}